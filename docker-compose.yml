version: "3.8"
services:
  job-scheduler:
    build: .
    image: job-scheduler-backend:latest
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - ./data:/usr/src/app/data
    restart: unless-stopped
